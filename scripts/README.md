# scripts/

Reserved for any local helper scripts (e.g. regenerating badges or previewing the profile locally).
No scripts are required for the current setup — all dynamic content (stats, streak, snake, 3D calendar) is generated by the GitHub Actions workflows in `.github/workflows/`.
